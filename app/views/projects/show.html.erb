<h2><%= title @project.name, "Projects" %></h2>
<%= link_to 'Edit Project', edit_project_path(@project) %>
<%= link_to 'Delete Project', projects_path(@project), method: :delete, data: {confirm: 'RLY?'} %>
